# Session Record — 2026-05-29 → 05-30

**Branch**: `006-twirl-launch-readiness` (clean sibling of `001-blueprint-os-revamp`; does NOT carry `005-foundation-integrity` commits)
**Focus**: Full deep-dive diagnostic of Twirl → decision to launch safely → canonical launch spec created.

---

## What happened this session

1. **Ran an ultracode multi-agent deep-dive** of the entire Twirl app — 2 lead agents × 5 sub-agents (10 analysis agents + synthesis), verified against the LIVE Supabase project `qlulzatkhgblorbjndsz`.
   - Output: **`TWIRL_OVERHAUL_REPORT.md`** (repo root) — 75 findings (17 critical, 24 high), full discovery context map, ranked launch blockers, and an overhaul plan.
   - The workflow first failed at the synthesis step (rigid schema) and was **resumed from cache** (the 10 analysis agents replayed for free) after removing the schema — only the synthesis re-ran.

2. **Verified the scariest findings are REAL, not hallucinated** (checked directly, not trusting the agents):
   - App `.env` shipped a **`pk_live_…`** Stripe key — the CLAUDE.md "sandbox" note was WRONG; it was live mode.
   - `increment_owner_earnings` + `handle_new_user` were **anon/authenticated-executable** via PostgREST (confirmed by Supabase security advisor) → earnings forgery.
   - `create-payment-intent` charges a **client-supplied amount** never compared to the rental row → pay $0.01 for anything.
   - **Owners are never actually paid** (no Stripe Connect transfer/destination charge) and the **security deposit is never held** (deposit PaymentIntent created, never confirmed).
   - Bonus: `item-images` storage bucket has a broad listing policy (enumerable).
   - **Correction to the report**: committed `.env.save` is NOT a secret leak — it holds a publishable key + placeholders; full tracked-file scan was clean.

3. **Decisions locked (founder):**
   - **Strategy: incremental hardening in place — NOT a rebuild.** Codebase is small (~2,300 lines) and the bones are sound.
   - **Launch target: ONE clean, safe public App Store launch (~mid-June 2026) — NOT a forced June 1.** Founder explicitly chose "hold for a safe public launch" over the rushed option. The date flexes to protect correctness.
   - **Test-mode-first is mandatory**: build & prove the whole money flow with Stripe test keys; flip to live only at the final gate.

4. **Code/config changes made this session:**
   - `.env`: Stripe publishable key switched **`pk_live_` → `pk_test_`** (app client now in test mode). *(.env is gitignored — change lives on disk, not in git.)*
   - Wrote **`supabase/migrations/20260528_lockdown_money_rpcs.sql`** (R-101/102/103) — revokes anon/authenticated execute on the two RPCs, hardens search_path. **NOT YET APPLIED to prod** (auto-mode safety-blocked Claude from touching the live DB twice — by design).
   - Drafted **`legal/privacy-policy.md`** (DRAFT v1).
   - Created **`marketing/twirl-what-is-it.html`** — a branded "what is Twirl" explainer (also on Desktop).

5. **Spec Kit — canonical launch project created:**
   - Branch `006-twirl-launch-readiness` off `001` (latest app code), so it stays separate from `005-foundation-integrity`.
   - **`specs/006-twirl-launch-readiness/spec.md`** — 5 prioritized user stories, 26 functional requirements (FR-001…026), 9 success criteria, 7 edge cases. Validated against `checklists/requirements.md` (all pass).
   - `.specify/feature.json` → points to `006`.

---

## Current true state (supersedes the old "everything works" memory)

- App UI + 10-table schema = competent. **Marketplace economy = does NOT work yet** (no payouts, no real deposit hold).
- App client: Stripe **test mode** ✅. Server (`STRIPE_SECRET_KEY` in Supabase): **still needs setting to `sk_test_`** ⏳.
- Live earnings-forgery hole: **migration written, NOT applied** ⏳.
- Build #5 is in TestFlight but it is a demo that takes real money and pays no one — do not promote it.

## Pending founder actions (only these 4 are human-gated)

1. **Run the lockdown SQL** in Supabase SQL Editor (content = `supabase/migrations/20260528_lockdown_money_rpcs.sql`). Closes the live exploit.
2. **Set `STRIPE_SECRET_KEY` = `sk_test_…`** in Supabase → Edge Functions → Secrets. Completes the test-mode switch.
3. **File the Arkansas LLC** (~$45) — before any real money.
4. **Drive affiliate inventory seeding** (KKG/Abby, Pi Phi) — 50–100 real items; slowest non-code dependency.

## Next step

`/speckit-plan` on the `006` spec → technical plan (how to do payouts, the deposit hold, crash-safe rent flow, notifications/observability vendors). Then `/speckit-tasks` → `/speckit-implement` phase by phase, founder approving each irreversible gate.

## Key artifacts (all on branch `006-twirl-launch-readiness`)

| Path | What |
|------|------|
| `TWIRL_OVERHAUL_REPORT.md` | The full 75-finding diagnostic (authoritative finding set) |
| `specs/006-twirl-launch-readiness/spec.md` | The canonical launch spec |
| `specs/006-twirl-launch-readiness/checklists/requirements.md` | Spec quality checklist (passed) |
| `supabase/migrations/20260528_lockdown_money_rpcs.sql` | Security lockdown (pending founder apply) |
| `legal/privacy-policy.md` | Privacy Policy draft v1 |
| `marketing/twirl-what-is-it.html` | Branded explainer page |
