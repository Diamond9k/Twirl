# Twirl — Mac → Windows + BridgeMind Handoff

> Generated 2026-06-05 from live inspection of the Mac (not from memory).
> Read-only audit except the §1 commit+push. Nothing was deleted on the Mac.
> **Rule honored:** no secret VALUES appear in this file — names/locations only.

---

## §0 / §1 — CODE IS NOW THE SINGLE SOURCE OF TRUTH ✅

- **Repo:** `https://github.com/Diamond9k/Twirl.git` (remote `origin`). Local: `~/Twirl-Hub/repo/Twirl`.
- **`main` HEAD (verify this matches on Windows):**
  `993a2f97a34cd26c8a27358eb16db427eef38e5b` — `2026-06-05 16:51:00 -0500`
- **What I committed** (`[handoff]` commit `993a2f9`): four untracked-but-real spec/doc trees that would have been lost —
  `specs/006-twirl-launch-readiness/{plan,research}.md`, `specs/005-foundation-integrity/baseline.md`,
  `.specify/specs/003-agent-os-game/{spec,plan,tasks}.md`. No secrets, no `.env`, no binaries.
- **Stashes:** none.
- **Local-only branches I pushed (the #1 move-risk):**
  - `005-foundation-integrity` — had **2 unique commits** not on the remote → now pushed. (Do NOT merge into `main`; it's 16 commits behind and would delete current work. It's a preserved snapshot only.)
  - `006-twirl-launch-readiness` — pushed for safety, but its work is **already fully merged into `main`** (0 commits ahead). `main` is ahead of it.
- **Branches already safe:** `main`, `001-blueprint-os-revamp` (merged), plus ~25 `origin/cursor/*` bug-investigation branches on the remote.
- **NOT committed (intentional):** `Screenshot 2026-05-23 at 9.32.54 PM.png` (1.1 MB binary, repo root) — disposable debug screenshot; grab it manually if wanted, see §9.

### ⚠️ Flag: `.env.save` is tracked in git (already on GitHub)
`.env.save` (mode 600) is committed. It contains only `EXPO_PUBLIC_*` values — the Supabase **anon** key and Stripe **publishable** key, which are public-by-design (they ship in the client bundle). The dangerous keys (Stripe **secret**, Supabase **service_role**) are NOT in it. Still recommend, on Windows: `git rm --cached .env.save && echo ".env.save" >> .gitignore`. You're rotating keys anyway, so urgency is low.

---

## §2 — TOOLCHAIN / RUNTIME

| Tool | Mac version | Notes for Windows |
|------|-------------|-------------------|
| Node | **v22.19.0** | via **nvm** (function present in `~/.zshrc`). No `.nvmrc` in repo → install Node 22.x on Windows (nvm-windows). |
| npm | **11.13.0** | **`package-lock.json` is authoritative.** No yarn/pnpm lockfiles. Run `npm ci`. |
| Expo SDK | **54** (`expo ~54.0.34`; CLI reports 54.0.24) | ✅ confirmed SDK 54. |
| eas-cli | **19.1.0** | `eas whoami` → **jimbo36** (`cporter2us@gmail.com`). Re-login on Windows: `eas login`. |
| TypeScript | ~5.9.2 | `npm run typecheck` = `tsc --noEmit`. |
| React / RN | react 19.1.0 / react-native 0.81.5 / react-native-web 0.21 | new-arch enabled (`newArchEnabled: true`). |

**Mac-only pieces Windows LOSES (iOS dev loop dies on Windows):**
- **Xcode 26.4.1**, **iOS Simulator** — cannot run on Windows at all.
- **CocoaPods 1.16.2** — only needed for the iOS native build.
- **Watchman:** absent on this Mac (not used).
- `/ios` folder is **gitignored & generated** (present locally with Pods/Twirl.xcworkspace). It is regenerated by `expo prebuild`. **Windows consequence:** you can develop JS, run `expo start`, and build via **EAS cloud** (which compiles iOS in the cloud — no Mac needed), but you **cannot** do a local iOS native build or run the iOS Simulator. Use Expo Go / a physical device + EAS builds.

---

## §3 — SECRETS & CREDENTIALS (names/locations only — reissue map)

### App env (`~/Twirl-Hub/repo/Twirl/.env`, gitignored)
All client-side, all `EXPO_PUBLIC_*` (safe to be public):
- `EXPO_PUBLIC_SUPABASE_URL` → Supabase dashboard → Project Settings → API
- `EXPO_PUBLIC_SUPABASE_ANON_KEY` → same (anon/publishable key)
- `EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY` → Stripe dashboard → Developers → API keys (`pk_test_…` now)
- `EXPO_PUBLIC_API_URL` → the Supabase functions base URL
- (`.env.example` = template, tracked. `.env.save` = stale dupe, see §1 flag.)

### Server-side secrets — NOT in the repo, set in **Supabase → Edge Functions → Secrets**
- `STRIPE_SECRET_KEY` (currently the test switch is the open item — see §10) — reissue: Stripe dashboard.
- Supabase `service_role` key — reissue: Supabase dashboard. Used only server-side.

### MCP / tooling secrets living **plaintext in `~/.cursor/mcp.json`** (mode 600) — must move + ROTATE
Variable NAMES only:
- `github.GITHUB_PERSONAL_ACCESS_TOKEN`
- `hostinger-mcp.API_TOKEN`
- `codex.OPENAI_API_KEY`
- `code-review.{ANTHROPIC_API_KEY, OPENAI_API_KEY, GITHUB_TOKEN}`
Already env-ref (not plaintext): `blueprint-os.OBSIDIAN_TOKEN`, `obsidian.OBSIDIAN_TOKEN`, `manus.MANUS_MCP_API_KEY` (from `~/.zshrc`).

### Build / store credentials
- **`eas.json` submit block:** Apple ID `cporter1us@hotmail.com`, `ascAppId 6765989926`, `appleTeamId JD84S94RNQ`. (Note: this Apple ID differs from the operator email `cporter2us@gmail.com`.)
- **iOS signing certs / provisioning profiles:** managed by **EAS** (portable — re-auth on Windows, no export needed). Nothing iOS-signing-related is Mac-local-only.
- No Android keystore yet (no `/android`, no `.jks`).
- Google Play service-account JSON: none found (not set up yet).

---

## §4 — BACKEND (Supabase) — live-verified, with DRIFT flagged

- **Project ref:** `qlulzatkhgblorbjndsz` · **Name:** Twirl · **Org:** `owqsiymxoalbqgntttdh`
- **Tables (10, all RLS enabled ✅):** profiles(2 rows), items(3), saved_items(0), conversations(1), messages(1), rentals(1), rental_contracts(0), reviews(0), reports(0), blocked_users(0). → tiny test data only; no real seed inventory yet.
- **Security advisors:** only **1 WARN** — leaked-password protection disabled (a dashboard toggle). RLS healthy.
- **`supabase/.temp/linked-project.json`** holds the link; `supabase login` is machine-local → **re-auth on Windows.** No `config.toml` in repo (baseline schema is `supabase/schema.sql`, 15 KB).

### ⚠️ Edge functions: 2 deployed functions have NO source in the repo
Repo has 4 function dirs: `create-connect-account`, `create-payment-intent`, `release-deposit`, `delete-account`.
**Live deployed = 6** (all ACTIVE): the 4 above **plus** `publish-legal` (v3) and `privacy` (v2) — the **retired** privacy-hosting attempt. Their source is dashboard-only (not in git). They're slated for deletion (privacy now lives on Hostinger Horizons), so this is cleanup, not loss — but note it.

### ⚠️ Migration history drift (CLI files ≠ live tracking table)
- **Local files (5):** `20260511_add_profile_demographics`, `20260524_release_deposit_rpc`, `20260528_lockdown_money_rpcs`, `20260604_account_deletion`, `20260604_r3_security_lockdown`.
- **Live migration table (3):** `20260524021002 release_deposit_rpc`, `20260604192919 r3_security_lockdown_rpcs_view_bucket`, `20260604210004 r13_account_deletion`.
- Cause: migrations were applied via the **MCP/dashboard** (which renames them), not `supabase db push`. The `20260528` lockdown was never applied as its own migration — its content was folded into the live `r3_security_lockdown`. `20260511` predates the tracked history (baked into `schema.sql`).
- **Windows takeaway:** schema source of truth = `schema.sql` + the migration files **together**. Expect `supabase db pull` to show history drift; don't blindly `db push`.
- No cron jobs / DB webhooks found.

---

## §5 — THIRD-PARTY SERVICES (reconnect, don't rebuild)

- **Stripe:** **test mode**, Express **Connect** configured. Account `acct_1TOc7bH9wFsk14vQ`. Money flow = app → Supabase edge fns (`create-payment-intent`, `create-connect-account`, `release-deposit`) calling Stripe directly. **No Stripe webhook endpoint** found in repo, and **no `stripe listen`/tunnel** in the dev loop — functions are invoked directly by the app, not via webhooks. (If you add webhooks later, that's new work.)
- **Expo / EAS:** owner **jimbo36**, slug **twirl**, `projectId c65a84e0-9ea3-4bbb-aa05-9706cb442756`, bundle id **twirl.rentals**. Push notifications: not configured.
- **Apple:** App Store Connect app id `6765989926`, team `JD84S94RNQ`. App not yet successfully submitted (EAS submission failures are a known blocker — see memory `project_eas_submission_pattern`).
- **Stripe merchant id:** `merchant.com.twirl.rentals` (app.json + entitlements aligned).
- **Hosting:** `twirl.rentals` is a **Hostinger Horizons** site (managed React SPA — no FTP docroot). `/privacy` is edited in the Horizons builder, source `legal/privacy-embed.html`. No analytics / Sentry / OAuth providers wired.

---

## §6 — DURABLE KNOWLEDGE (clean, still-true; for the fresh Second Brain)

**The old Obsidian vault is essentially empty (0 Twirl notes).** Nothing to extract there — all real knowledge is in-repo. Durable facts:

**What Twirl is:** P2P clothing-rental marketplace for U of Arkansas sorority women ("Airbnb for dresses"). 15% platform commission. Expo/React Native (iOS-first) + Supabase + Stripe Connect. Domain `twirl.rentals`, `@twirl.rentals`.

**Architecture & key decisions (+ why):**
- **Expo SDK 54 + expo-router v6** (file-based routing in `app/`). New architecture on.
- **NativeWind v4 + Tailwind 3** for styling; theme tokens in `tailwind.config.js` / `.twirl_theme.json` (pinks/rose, cream `#FBF7F0`).
- **Supabase** for DB+Auth+Storage+Edge Functions. Chose **Supabase Edge Functions over Railway** (HOR 8.5) to keep one backend.
- **Stripe Express Connect** (HOR 7.6) so owners get paid out directly; platform takes 15%, owner gets 85%.
- **Single-campus launch** (HOR 9.2) — U of A only first.
- **Money/security model:** the deposit must be *held* (not charged) and released via `release-deposit`; owner payout is 85%. The whole economy is gated behind **test-mode-first** proof.

**Gotchas (hard-won):**
- Edge-fn call URLs: use `${EXPO_PUBLIC_SUPABASE_URL}/functions/v1/<fn>` — `EXPO_PUBLIC_API_URL` already ends in `/functions/v1`, so concatenating doubled the path → 404 (bug #5, fixed in R-1).
- Supabase **cannot host HTML** — it rewrites `text/html`→`text/plain` on `*.supabase.co` (anti-phishing). Privacy page MUST live on Horizons, not Supabase. (This is why `publish-legal`/`privacy` edge fns are dead.)
- Migrations get renamed when applied via MCP/dashboard → history won't match filenames (see §4).
- macOS GUI apps (Cursor) don't source `~/.zshrc`, so `${VAR}` in `mcp.json` only resolves if the launcher sees the var — relevant when moving MCP secrets.

**Conventions:**
- `COMMISSION_RATE = 0.15` and the SEC-schools / occasions / sizes / categories enums live in `lib/constants.ts`.
- Spec-driven (speckit): `/speckit-specify → plan → tasks → implement`, feature branches per spec.

**Open questions:** EAS iOS submission root-cause; when to flip Stripe live; LLC timing; real inventory seeding.

---

## §7 — CURSOR → BridgeMind / Claude Code (recreate the AI workflow)

- **No `.cursorrules` / `.cursor/rules/` / `AGENTS.md` in the repo.** AI behavior is driven by:
  - `~/CLAUDE.md` — Blueprint OS v0.5 operating protocols (global, ~all sessions).
  - `~/Twirl-Hub/repo/Twirl/CLAUDE.md` — project pointer (says: read `TWIRL.md` first).
  - `TWIRL.md` — single catch-up file.
  - Claude memory: `~/.claude/projects/-Users-cooperporter-Twirl-Hub-repo-Twirl/memory/` (MEMORY.md index + fact files).
- **`.claude/settings.local.json`** (repo) — permissions/config.
- **`.claude/skills/`** — speckit slash commands (project-scoped).
- **`~/.cursor/mcp.json` — 10 MCP servers to re-add on Windows** (command + purpose; secrets per §3):
  | server | command | purpose |
  |---|---|---|
  | github | `npx @modelcontextprotocol/server-github` | repo/PR/issues |
  | supabase | remote `mcp.supabase.com/mcp` | DB/functions/advisors |
  | stripe | remote `mcp.stripe.com` | payments |
  | hostinger-mcp | `npx hostinger-api-mcp` | Horizons/domain |
  | higgsfield | remote `mcp.higgsfield.ai/mcp` | image/video gen |
  | codex | `npx codex-mcp-server` | OpenAI codex |
  | code-review | `npx @praneybehl/code-review-mcp` | code review |
  | blueprint-os | `node ~/cooperbrain-kit/mcp/blueprint-os-server.js` | Blueprint OS (9 tools) — **Mac path; re-point on Windows** |
  | obsidian | `node ~/.cooperbrain/obsidian-bridge.mjs` | vault bridge — **Mac path** |
  | manus | `npx manus-mcp-client` | Manus (needs API key) |

---

## §8 — DEV WORKFLOW & CUSTOM TOOLING

- **Day-to-day dev:** `npm run start` (= `expo start`); `npm run ios` / `npm run web`. iOS native prep (Mac-only): `npm run ios:prep` (`expo prebuild` + `pod install`) then `npm run ios:open`.
- **`live_editor.py`** (Tkinter GUI, ~25 KB): a 3-agent pipeline (Editor → Verifier → Corrector) using the `anthropic` SDK to make app edits with hallucination-checking. Run: `python3 live_editor.py` (needs `pip install anthropic` + an Anthropic key in env). **Mac/desktop GUI tool** — Tkinter works on Windows too.
- **`simulator.py`** (Tkinter GUI, ~85 KB): an in-house phone-frame UI simulator/themer; reads/writes `.twirl_theme.json`. Run: `python3 simulator.py`.
- **Background services needed:** none required for normal dev (no local Supabase, no `stripe listen`, no proxies). Expo dev server only.
- `@expo/ngrok` is a devDependency (tunnel for `expo start --tunnel` when needed).

---

## §9 — NON-GIT ASSETS

- **App assets (tracked, in `assets/`):** `icon.png`, `icon.svg`, `adaptive-icon.png`, `splash-icon.png`, `favicon.png`. Brand cream `#FBF7F0`.
- **Fonts:** Google Fonts via npm (`@expo-google-fonts/{cormorant-garamond, inter, jetbrains-mono}`) — no local font files / licenses to move.
- **`/ios`** — gitignored, generated, Mac-local (regenerated by `expo prebuild`). ~present with Pods.
- **`Screenshot 2026-05-23 …PM.png`** (1.1 MB, repo root, untracked) — debug screenshot; left in place, grab if wanted.
- **`.expo/`** — local cache, gitignored, disposable.
- **Marketing/legal HTML (tracked):** `legal/privacy{,-embed}.html`, `legal/privacy-policy.md`, `marketing/twirl-what-is-it.html`.
- **Big report (tracked):** `TWIRL_OVERHAUL_REPORT.md` (200 KB) — the 75-finding audit.

---

## §10 — CURRENT STATE (pick up here on Windows)

- **Active working branch is `main`** (HEAD `993a2f9`). ⚠️ `TWIRL.md` says "be on `006-twirl-launch-readiness`" and lists `/speckit-plan` as the next step — **that's stale.** Reality: the launch-readiness work (R-9…R-15: pickers, payout nudge, review flow, account deletion, privacy page) was committed straight onto `main`, and `specs/006-.../plan.md` + `research.md` already exist. `006` the branch is behind `main`.
- **Last things done (most recent first):** privacy page de-entitied (R-14), dead Supabase privacy fn removed / privacy moved to Horizons (R-15), in-app account deletion for Apple 5.1.1(v) (R-13), renter review flow (R-11), payout-setup nudge (R-10), real pickers (R-9).
- **Pending FOUNDER actions (human-gated):**
  1. ~~Run `20260528` lockdown SQL~~ — **DONE** (live as `r3_security_lockdown`; advisors clean). `TWIRL.md`'s pending-#1 is stale.
  2. **Set `STRIPE_SECRET_KEY = sk_test_…`** in Supabase → Edge Functions → Secrets — completes the server-side test-mode switch (app is already `pk_test_`). **Still open.**
  3. **File the Arkansas LLC** (~$45) before real money moves.
  4. **Seed real inventory** (KKG/Abby, Pi Phi → 50–100 items) — slowest non-code dependency.
- **Known blockers / next 1–3:** (a) EAS iOS submission failure — diagnose root cause (Apple ID `cporter1us@hotmail.com`); (b) finish test-mode money-flow proof (deposit hold + 85% payout) end-to-end; (c) then live-key flip.
- **The standing blocker — "needs Supabase + Stripe LIVE keys" — is STILL TRUE and INTENTIONAL.** Everything stays test-mode until the money flow is proven; live keys are a deliberate late gate, not a missing step.
- **Budget:** ~$389 remaining. Target: clean public launch ~mid-June 2026 (rush).

---

*Nothing on the Mac was deleted. Verify Windows runs Twirl end-to-end before wiping anything.*
